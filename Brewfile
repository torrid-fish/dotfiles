# Portable CLI tools (both Linux via apt and macOS via brew; this file is the macOS bootstrap)
brew "stow"
brew "git"
brew "gh"
brew "starship"
brew "tmux"
brew "vim"
brew "btop"
brew "yazi"
brew "cava"
brew "zsh" # macOS ships an ancient /bin/zsh; chsh -s "$(brew --prefix)/bin/zsh" after install

# GUI apps
cask "kitty"
cask "zed"

# CLI agents (pi, claude, codex, ...) install via their own channels — not managed here
